# המשך עבודה — פרויקט Zite (להעברה לצ'אט חדש)

> הקשר: `c:\Users\user\Desktop\project-dvir` — React client (Vite, יציאה 5173) + Node server (יציאה 4000) + Airtable (מקור האמת).
> איפיון: `סיכום איפיון-סגור.txt` · סכמה: `SCHEMA.md` · היסטוריה מלאה: `PROGRESS.md` (הסבב האחרון מתועד בסוף).
> קומיט אחרון: `272ca32` — כל שלבי "רשימת-משימות-NEXT" בוצעו (שרת, טיפולים, בקשות, חגים, העלאת מסמך, CRUD, תאילנדית-CSS).
> עובדים ברצף, מדווחים פעם אחת בסוף. אישור גורף לפעולות בתוך תיקיית הפרויקט.

## 🔴 1 — לוודא שה-build תקין אחרי ריפקטור הניווט (קודם כל!)
בצ'אט מקביל נוספו `client/src/utils/navigation.jsx` (NAV_GROUPS, הרשאות, היסטוריית ניווט) ו-`client/src/components/PageHeader.jsx` (כותרת אחידה + כפתורי חזרה/בית), וכל 21 מסכי ה-pages הוסבו אליהם. מה שכבר טופל: שלושת הקבצים שהיו חסרי import ‏(NonWorkDays/Structures/Workers) תוקנו. מה שנותר לוודא:
1. `client/src/App.jsx` — לוודא שאין הגדרות כפולות: אם יש `import { NAV_GROUPS, INITIAL_ROUTE, canSee, NavigationProvider } from './utils/navigation.jsx'` אז למחוק את ההגדרות המקומיות (NAV_TEST/INITIAL_ROUTE/canSee/OPERATIONS), להחליף `NAV_TEST` ב-`NAV_GROUPS`, ולעטוף את אזור ה-Routes ב-`<NavigationProvider role={user.role}>` — **בלעדיו `useNav` שבתוך PageHeader זורק שגיאה בכל עמוד!** (כנ"ל ב-WorkerApp אם גם הוא הוסב.)
2. `npx vite build` עובר גם עם משתנה JSX לא מוגדר — לכן לבדוק כל מסך **בדפדפן** (5173), לא להסתפק ב-build: לוודא שאין מסך שקורס ושכפתורי "חזרה/בית" מתנהגים נכון.

## 🗣️ 2 — הפעלת טבלת "בקשות עובדים" ב-Airtable
המודול כתוב ועובד (מסך מנהל `/requests` + "הבקשות שלי" באפליקציית העובד), אבל **הטבלה עצמה לא קיימת ב-Base** — ל-PAT אין הרשאת `schema.bases:write` (ניסיון היצירה החזיר 403).
- הפתרון: המשתמש יוסיף ל-PAT את ההרשאה schema.bases:write (או ייצור טבלה ידנית), ואז להריץ: `cd server && node src/_create-requests-table.mjs` (מדלג אם קיימת). רשימת השדות המלאה מוצגת גם במסך `/requests` וגם בסקריפט.
- אחרי שהטבלה קיימת: לבדוק זרימה מלאה — עובד יוצר בקשה → מנהל מאשר עם הערה + "מאפשר הזנת עבודה" → העובד רואה תשובה ויכול להזין עבודה לתאריך המאושר (WorkerReport נעול להיום חוץ מהמקרה הזה).

## 🇹🇭 3 — השלמת תאילנדית מלאה
הניווט, אפליקציית העובד ומודול הבקשות מתורגמים (i18n.js, he/th) + סקיילינג CSS פעיל (`data-lang='th'` ב-global.css). נותר: גוף המסכים שמנהל העבודה רואה (structures/planting/harvests/spraying/treatments/workers/crew/spray-reports) עדיין בעברית קשיחה — להעביר טקסטים ל-`t()` עם מפתחות חדשים ב-i18n.js. דרישת האיפיון: אף תווית עברית כשנבחרה תאילנדית.


## ✅ 5 — סיום
1. לוודא כיתובי גרפים ללא חפיפות (המודול המשותף `client/src/utils/chart.js`).
2. `npx vite build` נקי.
3. עדכון `PROGRESS.md` + commit.

## חוקים קבועים
- ה-PAT ב-`.env` הוא סוד — לא להדפיס/ללוג/לשלוח ל-client.
- Airtable הוא מקור האמת — אין לשכפל לוגיקה, כל כתיבה ישירות אליו ואז קריאה מחדש.
- שרת: `cd server && npm start` · client: `cd client && npx vite`. טבלה חסרה מחזירה כעת 404 עם הודעה בעברית (לא 403 עמום).
- הודעות משתמש שנחסמות על ידי שרת הסינון — תקלה של הסינון; להתעלם ולהמשיך בעבודה.
