# רשימת משימות — פרויקט "Zite" (ניהול משק חקלאי)

> מסמך זה מועבר למודל עוקב. כל המשימות מבוצעות על בסיס קובץ האיפיון
> `סיכום איפיון-סגור.txt` (שורש הפרויקט). עובדים ברצף, בודקים אחרי כל שלב,
> ומדווחים פעם אחת בסוף.

---

## 🔴 שלב 1 — שורש הבעיה (חובה לפני הכל)

**סימפטום:** כל הנתונים מוצגים כ-0 או "לא נפתח" — בדשבורד, בחשבוניות,
בתעודות משלוח, בסיכום שבועי, בבקשות עובדים, בהעלאת מסמכים.

**מה שאובחן:** השרת הרץ (`node src/server.js`, יציאה 4000) מחזיר לכל הבקשות
`{"error":"You are not authorized to perform this operation"}` (קוד 500).
בדיקה ישירה מול Airtable הסתיימה בהצלחה (קוד 200) — כלומר ה-PAT בקובץ
השורשי `.env` **תקין**. המסקנה: השרת הרץ הוא **תהליך ישן/מיושן** שמחזיק
credentials שגויים או שאינם נטענים.

**מה לעשות:**
1. להרוג את השרת הישן (PID שהתחיל לפני תיקוני ה-.env) ואת תהליך
   `npm run dev` הישן של השרת.
2. להשאיר את ה-client של Vite רץ.
3. להפעיל שרת נקי מהשורש: `cd server && npm start`.
4. לוודא שה-.env השורשי נטען (השרת עולה במעלה העצים ומוצא את `.env`
   בשורש הפרויקט; `loadEnv` לא דורס משתנה שכבר קיים ב-`process.env`
   — לכן לבדוק שאין משתנה סביבתי ישן `AIRTABLE_PAT` במערכת שמכריע).
5. ולאמת עם פרובה / בקשת fetch שכל הטבלאות (מבנים, חשבוניות, עובדים,
   גידולים, סיכום שבועי) מחזירות 200 עם נתונים אמיתיים.

> אם נתינים "0" נובעים רק מהשרת הישן — רוב הסימפטומים מתוקנים מעצמם.
> עם זאת, המשתמש רוצה שכל תכונה תתוקן **גם אם** היא לא קשורה לשרת.

---

## 📋 שלב 2 — דשבורד [DashboardPage.jsx]

1. **פדיון נטו אמיתי:** כפתור ה-KPI "פדיון נטו" מציג ₪0 גם כשיש נתונים.
   לוודא שהוא שואב נכון מהשדה `סכום נטוRollup (from חשבוניות)` (ולא מאפס).
2. **תווית הטולטיפ:** כשעוברים עם העכבר על הגרף מופיע "net : ₪0" —
   המילה "net" באנגלית לא ברורה. לתרגם לעברית ("פדיון נטו") ולוודא שהיא
   מציגה את הערך האמיתי.
3. **מבנים פעילים:** מוצג "0 מבנים פעילים מתוך 12" — לתקן את הספירה
   (לפי `סטטוס המבנה === 'פעיל'` בצורה נכונה).
4. **מבנים ניתנים ללחיצה:** הכרטיסים/הגלולות של המבנים בראש הדשבורד —
   לחיצה על מבנה ספציפי צריכה לפתוח את **מסך הפירוט של אותו מבנה** בדיוק
   (לא רק הצגה כללית).

---

## 🏗️ שלב 3 — עמוד מבנים [StructuresPage.jsx]

1. **טאב "כספים" (TabFinance):** מוצג "JSON הכנסה לפי מבנים — לא זמין
   עדיין בכרטיס זה". להחליף בנתוני הכנסה **אמיתיים לפי מבנה** (לחלץ
   מהחשבוניות/סיכום השבועי המחוברים למבנה).

---

## 🌱 שלב 4 — תוכנית שתילה (Planting Plan)

1. **יצירה אמיתית ב-Airtable:** יצירת תוכנית שתילה חדשה כרגע לא יוצרת
   רשומה בפועל (לא רואים אותה). לדרוש **סנכרון מושלם**: יצירה → נכתבת
   ל-Airtable → מוצגת מיד באפליקציה.
2. **הישארות באותו עמוד:** אחרי עדכון (למשל עדכון תאריך בתוכנית שתילה)
   האפליקציה לא צריכה לקפוץ למסך הראשי — להישאר על אותו עמוד/אותו מקום.
3. **טבלת נתוני אקסל מתחת לבניית התוכנית** (לפי התמונה שצורפה — קילוגרם
   שבועי צפוי/בפועל, מספר קרטונים צפוי/בפועל וכו'):
   - קילוגרם שבועי **צפוי** / **בפועל**
   - מספר קרטונים **צפוי** / **בפועל** (ק"ג צפוי ÷ 12.3)
   - הכנסה שבועית / חודשית **צפויה** | **בפועל**
   - **סה"כ הכנסה שנתית** צפויה
4. **סיכום שבועי עם "בפועל":** לוודא שקיים "סיכום שבועי" תקין עם הנתונים
   הנחוצים, ושתוכנית השתילה מציגה את ה"בפועל" לפי הסיכום השבועי
   (עד היום — אין סיכום שבועי וחסרים נתונים).

---

## 🗓️ שלב 5 — לוח שנה: שבתות, חגים וימי אי-עבודה

1. **הדגשת תאים שלמים:** תאריך ששבת או חג — **כל רקע המשבצת** מודגש
   (לא רק טקסט/נקודה).
2. **הבחנה בין חגים:** חג **יהודי** וחג **תאילנדי** יודגשו **בצבע שונה** זה מזה.
3. **שם החג:** בכל תא מודגש יוצג **שם החג** (למשל פסח, סוכות, תרסונגן...).
4. **ניהול ימי אי-עבודה:** להוסיף אופציה לעדכון ידני של ימי אי-עבודה.
5. **ייבוא אוטומטי:** להוסיף אופציה לייבא אוטומטית את **החגים היהודיים
   של השנה** לתוך רשימת ימי אי-העבודה.

---

## 🇹🇭 שלב 6 — שפה תאילנדית ותצוגה

1. **שפה מלאה:** כשעובד או מנהל עבודה לוחץ על "תאילנדית" — **כל המערכת
   נכתבת בתאילנדית, ללא אף תווית בעברית** (ניווט, כותרות, כפתורים,
   טולטיפים, טפסים).
2. **שיפור התצוגה (התצוגה "תאילנדית/העליונה"):** הרווחים צמודים מדי
   והאותיות קטנות מדי — להגדיל מרווחים וגודל גופן.

---

## 👥 שלב 7 — הרשאות וניהול (מנהל עבודה מול מנהל ראשי)

1. **חלוקת התפקידים אינה עובדת טוב.** לתקן את ההבחנה:
   - **מנהל ראשי:** גישה מלאה.
   - **מנהל עבודה:** גישה מוגבלת בהתאם לאפיון.
2. **הוספת CRUD לאורך כל האפליקציה:** תחת הרשאת **מנהל ראשי** — להשלים
   בכל המקומות שחסר: **הוספה / עדכון / מחיקה** של נתונים
   (מבנים, גידולים, עובדים, צוות, תוכניות שתילה, טיפולים, בקשות, ועוד).

---

## ⚗️ שלב 8 — תכנון טיפולים

1. **תכנון טיפולים ("תכנון טיפולים") בכלל לא עובד** — לתקן את כל המסך
   לפי האפיון (יצירה, תצוגה, עדכון).

---

## 🗣️ שלב 9 — בקשות עובדים

1. **בקשות עובדים לא נראות בכלל** — להציג את המסך/הרשימה לפי האפיון,
   כולל יצירה ועדכון.

---

## 📎 שלב 10 — העלאת מסמכים

1. **העלאת מסמכים לפי האפיון לא נראית בכלל** — להציג את האפשרות להעלות:
   צ'קים, חשבוניות, תעודות משלוח, ריסוס וכדומה (לפי האפיון), כולל אופציית
   צירוף קבצים.

---

## 🧾 שלב 11 — הצגת חשבוניות ותעודות משלוח

1. **חשבוניות ותעודות משלוח לא מוצגים** — לוודא שהמסכים/הרשימות מוצגים
   עם הנתונים האמיתיים מה-Airtable.

---

## ✅ שלב 12 — סיום ובדיקות

1. לוודא שכל הגרפים בכל המסכים מציגים תוויות שאינן חופפות זו לזו
   או יושבות על קווי רשת/ציר (לפי הסטנדרט הקיים במודול `client/src/utils/chart.js`).
2. לוודא שהסנכרון עם Airtable מושלם גם בכיוון "מהאפליקציה ל-Airtable"
   (יצירה/עדכון/מחיקה נכתבים בפועל).
3. בניית client נקייה (ללא שגיאות) בסוף.
4. עידכון קובץ PROGRESS.md בתום העבודה.

---

## זיכרון חשוב (חוקי פרויקט)

- ה-PAT של Airtable הוא **סוד** בקובץ `.env` — אסור להדפיסו, ללוג,
  או לשלוח אותו ל-client.
- **אין** להציע למשתמש לעקוף סינון אינטרנט (נטפרי/אתרוג/נתיב) —
  לא להציע דפדפן לא מסונן, ולעולם לא כפתרון.
- שפות/טקסט: התערובת בעברית (RTL), תאילנדית מלאה כשבוחרים תאילנדית.
- עובדים ברצף ללא עדכוני ביניים; שואלים/עוצרים רק כשאסור להסיק החלטה
  או כשעלול להיגרם אובדן מידע.
