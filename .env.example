# ============================================================
# Zite — Environment Variables (דוגמה, מלא ערכים אמיתיים בשרת)
# ============================================================

# Airtable
AIRTABLE_PAT=
AIRTABLE_BASE_ID=

# Server
PORT=4000
NODE_ENV=production

# Client (Vite — נטען ב-build time)
VITE_API_URL=
