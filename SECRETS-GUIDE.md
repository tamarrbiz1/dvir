# 🔐 מדריך אבטחת הטוקן של Airtable — תיעוד חובה

> מסמך זה מיועד למפתח/ת המערכת ולמנהל/ת המערכת.
> **יש לשמור אותו בתיקיית הפרויקט, ולקרוא שוב לפני נגיעה בקוד.**

---

## 🎯 תקציר (חוק הזהב)

**הטוקן (API Key / Personal Access Token) של Airtable הוא קוד-סודי — כמו סיסמה או מפתח בית.**
אדם שבידיו הטוקן יכול לקרוא ולשנות את כל הנתונים של בסיס Airtable.

לכן ישנם שלושה חוקים בלתי־נשברים:
1. **לעולם לא לשים את הטוקן בקוד** (לא בקובצי JS/TS, לא ב־React, לא ב־Node).
2. **לעולם לא להעלות את הטוקן ל־"git"/GitHub.**
3. **לעולם לא להכניס את הטוקן לקובץ שהדפדפן מראה למשתמש** (frontend).

הטוקן **חי רק בצד השרת** (Backend), בין אם זה המחשב שלך בלוקאלי ובין אם ענן.

---

## 📁 איפה בדיוק מניחים את הטוקן?

| שלב הפיתוח | איפה שמים את הטוקן | קובץ/מקום מדויק |
|---|---|---|
| **פיתוח מקומי** (בלי שרת, על המחשב) | קובץ `.env` מקומי | בתיקיית ה־`server` (backend) של הפרויקט |
| **בענן** (כשהמערכת נפרסת) | הגדרות סוד בפלטפורמת האירוח | Env / Secrets / Environment Variables של Railway / Render / Vercel / AWS |

### צורת הכתיבה בקובץ `.env`:
```bash
AIRTABLE_PAT=pat*****....הטוקן הארוך שלך
AIRTABLE_BASE_ID=appXXXXXXXXXXXXXXXX
AIRTABLE_TABLE_ID=tblXXXXXXXXXXXXXXXX
```

### למה זה בטוח?
- הקובץ `.env` **לא נכלל** בגרסה (יש לו רישום ב־`.gitignore` — אתן לך זאת).
- הקוד **קורא** את הערך מהקובץ עם `process.env.AIRTABLE_PAT` — לא כותב אותו.
- בפריסה לענן, הפלטפורמה מחזיקה את הסוד אצלה, **מחוץ לקבצי הפרויקט**.

---

## ❌ מה אסור לעשות לעולם

| פעולה אסורה | למה |
|---|---|
| לכתוב את הטוקן ישירות ב־`server.js` / `config.js` | זה נכנס לקבצי הקוד ולגרסאות |
| להדביק את הטוקן בקומפוננטות React | הדפדפן של כל משתמש יראה אותו |
| להעלות קבצים עם הטוקן ל־GitHub/OneDrive שלי | זה כבר לא סוד — זה פרצה |
| לשלוח את הטוקן בהודעת הצ'אט | אין צורך; שומרים אצלך בלבד |
| להדפיס את הטוקן ב־`console.log` | בכל המחצבה מדליפים אותו |

---

## 🧪 איך לבדוק שהקובץ שלך בטוח?

הריצי בקצה השרת פקודה קלה לבדיקה (לאחר שהפרויקט קורא את הסביבה):

```bash
# אם הכל בריא, יודפס: "טוקן קיים ✓ (מאורך ****)" ואחרת: "⚠️ טוקן חסר"
node -e "const t=process.env.AIRTABLE_PAT; console.log(t?('טוקן קיים ✓ (מאורך '+t.slice(-4))+'...'):'⚠️  טוקן חסר')"
```

> ⚠️ **חשוב:** לעולם אל תדפיסי את הטוקן המלא. רק בדיקה אם קיים.

---

## 🔄 אם הטוקן נדלף (לרעה)

1. נכנסי ל־Airtable → חשבון → **Tokens / API keys**.
2. **בטלי** את הטוקן שנחשך.
3. **צרי טוקן חדש** עם הרשאות מצומצמות (ריק) — רק לאותם טבלאות שדרושות.
4. עדכני את קובץ `.env` (או את הסוד בענן) בטוקן החדש.

---

## 📌 הכלל האחרון שחוזר תמיד

> **הטוקן = סוד. הסוד שייך ל־Server בלבד.**
> שום קבץ קוד, שום git, שום דפדפן — לא נוגעים בסוד מלבד צד השרת.

---

*המסמך הידור כחלק מהקמת מערכת ניהול המשק החקלאי. לפני שפורסת לענן — קראי שוב את "חוקי הזהב".*
