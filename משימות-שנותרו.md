# משימות שנותרו — פרויקט Zite (להעברה לצ'אט חדש)

> הקשר: תיקייה `c:\Users\user\Desktop\project-dvir`, React client (Vite) + Node server (יציאה 4000), נתונים ב-Airtable.
> האפיון בקובץ `סיכום איפיון-סגור.txt`. הרשימה המקורית המלאה ב-`רשימת-משימות-NEXT.md`, היסטוריה ב-`PROGRESS.md`.
> עובדים ברצף, מדווחים פעם אחת בסוף. אישור גורף לפעולות בתוך תיקיית הפרויקט.

## מצב נוכחי (נעשה, טרם commit)
- ✅ מסך חדש **"הכנסות ותחזית"** — `client/src/pages/FinancialForecastPage.jsx`, משולב ב-`App.jsx` (נתיב `/finance-forecast`) וב-`i18n.js` (`financeForecast`).
- ✅ `DashboardPage.jsx` — פדיון נטו/ברוטו/משקל מחושבים מטבלת **חשבוניות** (`סכום נטו`, `תאריך-AI`) במקום Rollup ריק; נוסף `useNavigate` למבנים לחיצים.
- ✅ `StructuresPage.jsx` — טאב כספים עם הכנסה אמיתית לפי מבנה.
- ✅ `PlantingPlanPage.jsx` — תוקנה כפילות `DonutCard`: הבלוק השגוי הפך ל-`function ExcelTable({ weeks, weekTotals })` (~שורה 1504), נוסף `KG_PER_CARTON = 12.3` (שורה 30), הטבלה מחוברת מתחת ל-`<Dashboard>` בטאב `dash` (~שורה 817).
- ✅ `npx vite build` עובר נקי (629 מודולים).

## 🔴 שלב 1 — שרת (חובה קודם)
1. להרוג שרת ישן שמחזיר `"You are not authorized"` (500); `cd server && npm start` נקי.
2. לוודא שאין משתנה סביבה מערכתי `AIRTABLE_PAT` שדורס את `.env` השורשי.
3. לאמת ש-`/api/מבנים`, `/api/חשבוניות`, `/api/עובדים`, `/api/גידולים`, `/api/סיכום שבועי` מחזירים 200 עם נתונים.

## 📋 שלב 2 — דשבורד (בדיקה מול נתונים חיים)
1. KPI "פדיון נטו" מציג ערך אמיתי (לא ₪0).
2. טולטיפ הגרף — "פדיון נטו" בעברית, לא "net".
3. ספירת "מבנים פעילים מתוך 12" לפי `סטטוס המבנה === 'פעיל'`.
4. לחיצה על מבנה בדשבורד פותחת את כרטיס אותו מבנה במסך מבנים (לוודא ש-StructuresPage קולט state/פרמטר ופותח את ה-Drawer הנכון).

## 🌱 שלב 4 — תוכנית שתילה
1. יצירת תוכנית חדשה נכתבת ל-Airtable ומופיעה מיד.
2. אחרי עדכון (למשל תאריך) — להישאר באותו עמוד.
3. לבדוק את `ExcelTable` בדפדפן. **באג ידוע:** הקיבוץ החודשי משתמש ב-`now.getFullYear()` ובתווית חודש של "היום" — לתקן לחודש/שנה של `w.start`.
4. "בפועל" נשאב מסיכום שבועי — לוודא שקיימים נתונים.

## 🗓️ שלב 5 — לוח שנה
1. רקע תא מלא לשבת/חג.
2. צבע שונה לחג יהודי מול תאילנדי.
3. שם החג בתוך התא.
4. עריכה ידנית של ימי אי-עבודה.
5. ייבוא אוטומטי של חגי ישראל לשנה.

## 🇹🇭 שלב 6 — תאילנדית
1. כיסוי 100% — אף תווית עברית כשנבחרת תאילנדית.
2. הגדלת גופן ומרווחים בתצוגה התאילנדית.

## 👥 שלב 7 — הרשאות
1. מנהל ראשי (מלא) / מנהל עבודה (מוגבל לפי אפיון).
2. השלמת CRUD למנהל ראשי בכל המסכים: מבנים, גידולים, עובדים, צוות, תוכניות שתילה, טיפולים, בקשות.

## ⚗️ שלב 8 — תכנון טיפולים: המסך לא עובד — יצירה/תצוגה/עדכון לפי אפיון.
## 🗣️ שלב 9 — בקשות עובדים: לא נראות — רשימה + יצירה + עדכון.
## 📎 שלב 10 — העלאת מסמכים: צ'קים/חשבוניות/תעודות/ריסוס עם צירוף קבצים (`POST /api/upload-document` קיים בשרת).
## 🧾 שלב 11 — חשבוניות ותעודות משלוח: מוצגות עם נתונים חיים (תלוי בשלב 1).

## ✅ שלב 12 — סיום
1. כל הגרפים — כיתובים ללא חפיפה (`client/src/utils/chart.js`).
2. סנכרון דו-כיווני עם Airtable.
3. `npx vite build` נקי.
4. עדכון `PROGRESS.md` + commit (שינויים ב-6 קבצים + קבצים חדשים).

## חוקי פרויקט
- ה-PAT ב-`.env` הוא סוד — לא להדפיס/ללוג/לשלוח ל-client.
- עברית RTL; תאילנדית מלאה בבחירה.
- לשאול רק לפני מחיקת נתונים חיים ב-Airtable.
