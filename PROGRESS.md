# 📋 תיעוד התקדמות — מערכת "משק חקלאי" (Zite over Airtable)

> פתח קובץ זה (או בקש ממני לקרוא) בכל שיחה חדשה כדי להמשיך בדיוק מהנקודה האחרונה.

- **פרויקט**: `c:\Users\user\Desktop\project-dvir`
- **ארכיטקטורה**: React (Vite) client + Node/Express server + Airtable (מקור האמת)
- **מקור האפיון**: [`סיכום איפיון-סגור.txt`](./סיכום איפיון-סגור.txt) · **מפת טבלאות**: [`SCHEMA.md`](./SCHEMA.md)
- **הרצה**: `start.bat` (dev server + client)

---

## ✅ הושלם — אפליקציית עובד + הרשאות (P1)

- **הרשאות 3 רמות** (App.jsx): `owner` / `manager` / `worker`.
  - owner: כל המסכים. manager: ניתוב בסרגל מצומצם לתפעול בלבד (לא רואה כספים/הכנסות). worker: עובר לאפליקציית Mobile נפרדת.
  - ניתן לגשת לאפליקציית עובד גם מהמסלול `/worker` (לבדיקת בעל העסק).
- **אפליקציית עובד** (Mobile-first): `client/src/worker/`
  - `WorkerApp.jsx` — Shell עם Header, LanguageSwitcher (עברית/ไทย), וסרגל תחתון (בית/הרווחים/דיווח/יציאה).
  - `WorkerHome.jsx` — מסך בית: "הרווחתי החודש/החודש הקודם", שעות החודש, עבודות החודש + רשימת עבודות אחרונות.
  - `WorkerEarnings.jsx` — "הרווחים שלי": פילטר מתאריך/עד + תצוגת חודש/שבוע/יום, KPI (הכנסה, שעות, עבודות, ממוצע ליום), גרף (בחירת פילוח יום/שבוע/חודש) + טבלה מפורטת.
  - `WorkerReport.jsx` — "דיווח עבודה": טופס (תאריך, מבנה, סוג עבודה, כמות, שעת התחלה/סיום, הערות) — יוצר רשומת "עבודות עובדים" ב-Airtable. העובד נקבע לפי המשתמש המחובר. תווית "כמות" דינמית לפי יחידת תמחור.
- **i18n**: הוסף מפתחות עברית/תאילנדית עבור כל אפליקציית העובד (`i18n.js`).

## ✅ הושלם — כרטיסים מלאים (P2)

- **WorkersPage** (כרטיס עובד מלא, סעיף 12): Grid כרטיסים (שם, סטטוס, שעות החודש, הכנסה החודש/הקודם, עבודות) → לחיצה פותחת Drawer עם KPI (שעות היום/השבוע/החודש, הכנסה החודש/הקודם, עבודות), פילטר מתאריך/עד, סה"כ בתקופה, ממוצע ליום, ו-6 גרפים.
- **StructuresPage** (כרטיס מבנה, סעיף 11): Grid כרטיסים → Drawer עם 8 טאבים (סקירה/תוכנית שתילה/עבודות/קטיפים/ריסוסים/תפוקה/כספים/מסמכים) — כל טאב עם נתונים מסוננים לפי המבנה. פירוט גיאומטרי וסקיצה בסקירה.
- **InvoicesPage** (סעיף 28): KPI ראשי + טבלה → Drawer חשבונית: KPI, פרטים, פירוק "סיכום יומי" (טבלה + גרף פדיון לפי יום + עוגת משקל לפי זן).
- **DeliveryNotesPage** (סעיף 29): KPI + טבלה → Drawer תעודה: פרטים, פירוק "סיכום יומי" (days/products) + גרפים.

## ✅ הושלם — צוות עובדים + תמחור (P3)

- **TeamCrewPage** (`/crew`, סעיף 13): פילטרים (שנה/חודש/מתאריך/עד), KPI (עובדים פעילים, שעות, תשלום, ממוצע), Grid כרטיסי עובדים, ו-5 גרפים (משכורת לפי עובד Horizontal, משכורות לאורך זמן, שעות לפי עובד, עלות לפי סוג עבודה, עלות לפי מבנה).
- **PricingPage** (`/pricing`, סעיפים 16–17): טאב תמחור עבודות (טבלה נקייה + פילטרים זן/סוג/יחידה) וטאב תמחור לפי מבנים (טבלה + Drawer גיאומטרי בלבד).

## ✅ הושלם — גידולים + תחזיות + ימי אי עבודה (P4 + P8)

- **CropsPage** (`/crops`, סעיפים 37–40): טאבים — גידולים (כרטיסים), מחירי גידול (טבלה + פילטר + גרף לאורך זמן), תפוקה רבעונית (Matrix Q1–Q4), תחזית שתילה (טבלה + גרף צפוי/בפועל).
- **NonWorkDaysPage** (`/nonworkdays`, סעיף 41): רשימת ימי אי עבודה (תאריך + סוג חג).

## ✅ הושלם — חריגות + סיכום שבועי (P5 + P6)

- **AlertsPage** (סעיף 35): ניתוח חריגות משדות ה-JSON לבקרת "סיכום שבועי" (סטטוס מסמכים/קטיף, שגיאת חישוב, רשימות הערות) + KPI, גרפים (לפי סוג/לפי שבוע), רשימה מפורטת, מלאי נמוך, ו-Drawer שבוע עם פרטי התאמה (ללא JSON גולמי).
- **WeeklySummaryPage** (סעיפים 30–34): רשימת שבועות עם סטטוסים → Drawer עם 6 טאבים (סקירה/לפי ימים/זנים/מבנים/ק"ג בפועל/התאמות) — פרסור נקי של שדות ה-JSON.

---

## ✅ הושלם בסבב נוסף
- **SprayingPage**: טופס ריסוס חדש פונקציונלי מלא (תאריך, מבנה, חומר עם ברירת־מחדל של מינון, מבצע, סוג/גודל מרסס, מרקם, בסיס מינון, מינון, הערות) + כפתור "סמן בוצע" לכל ריסוס (מעדכן Airtable). ראה סעיף 19.
- **FinancePage**: הורחב ל-4 טאבים — סקירה / הוצאות (KPI + טבלה + גרף חודשי + עוגת ספקים) / צ'קים (תצוגת מבוטלים + KPI) / משווקים (כרטיסים). סעיפים 10 + 25–27.
- **TreatmentsPage** (סעיף 22): לוח שנה אמיתי — תצוגת חודש (Calendar), תצוגת שבוע (7 ימים), תצוגת רשימה. תומך בטווחי תאריכים. Badge "היום".
- **SprayReportsPage** (סעיף 21): מסך ייעודי חדש — מציג תוצאות מפוענחות מ-Attachment Summary/AI ככרטיסים. חובר לניווט.
- **SuppliersPage** (סעיף 24): הורחב — Drawer עם 4 טאבים: פרטים / הוצאות / צ'קים / מלאי קשור.
- **שדות גמישים**: קובץ עזר חדש `client/src/utils/field.js` (pick/num/name) — רכיבי הכספים והחשבוניות משתמשים בו, כך ששינויי שמות/סוגי שדות ב-Airtable (הוצאות/חשבוניות/תעודות) לא שוברים את המסך; יוצג "לא זמין" או ערך חלופי.
- **ייסור לשמות שדות חיים (מ-Airtable במומנטו)**: החיבור ל-Airtable עובד (דרך ספריית `airtable`, לא דרך `fetch` ל-SCHEMA). בוצע יישור מול שמות השדות האמיתיים:
  - **תעודות משלוח**: `תאריך תעודה` / `כמות קרטונים` / `משקל כולל` / `משקל ממוצע לקרטון` / `סטייה מממוצע 12.3`.
  - **תמחור עבודות לפי מבנים**: עמודה `מבנים` + גיאומטריה מ-`(...from מבנים)`.
  - **מחירי גידול**: `מחיר משוער לקג` / `מחיר גידול משוער`.
  - **תחזית שתילה שבועית**: כעת נקרא מהטבלה הייעודית `תחזית שתילה שבועית` (18 שדות: `ק"ג צפוי`, `קג בפועל`, `הכנסה צפויה`, `קג לדונם לשבוע (from תפוקה רבעונית)`, `מחיר לקג מעודכן`...).
  - **תפוקה רבעונית**: `קג לדונם לשבוע`.
- הערה: חלק מהדפים המבוססים על נתוני עובדים/קטיפים תואמים לשמות החיים (נבדק). קובץ זמני `server/src/_fields-probe.mjs` (הדפסת שמות שדות חיים) — ניתן להחיק/להשאיר; אינו נוגע בייצור.

## ✅ סבב תשתית — תיקון הביצועים והקישורים (2026-08-26)

**הבעיה שדווחה:** "האפליקציה עובדת מאוד חלש, ללא קישורים מתאימים".
**מדידה לפני:** `/api/עבודות עובדים` עם 5 רשומות בלבד — **10.1 שניות**.

**שורש הבעיה** (`server/src/resolve-links.js`) — שני באגים בפתרון שדות-קישור:
1. טבלת היעד של שדה קישור נקראה מ-`options.recordLinkFieldId`, מאפיין שאינו קיים
   בשדה מסוג `multipleRecordLinks`. המאפיין הנכון הוא `options.linkedTableId`.
   התוצאה: טבלת היעד יצאה `null` **בכל** שדה קישור, וכל מזהה נפל ל-`findAlternativeOwner`
   שסרק עד 25 טבלאות בקריאת רשת נפרדת לכל אחת — עד 25 קריאות רשת לכל מזהה בודד.
2. שדות `multipleLookupValues` (שמחזירים ערכים, לא מזהים) טופלו כשדות קישור,
   ו-`recordLinkFieldId` שלהם הצביע על הטבלה הנוכחית ולא על טבלת היעד.

**התיקון:** פתרון batch — לכל טבלת יעד נטען אינדקס אחד (`id -> שם תצוגה`) ונשמר במטמון
ל-5 דקות, וכל המזהים נפתרים בזיכרון. אפס קריאות רשת לכל מזהה.
- נוסף טיפול ב-lookup שמושך שדה-קישור (החזיר Record IDs גולמיים ללקוח, למשל
  `תוכניות שתילה (from מבנה)`) — נפתר דרך השדה הפנימי.
- `DISPLAY_BY_TABLE` — שם תצוגה מפורש לכל 25 הטבלאות (למשל עובד = שם פרטי + משפחה),
  עם נפילה לשדה הראשי מהמטא. `labeled()` מונע כפילות כמו "מבנה מבנה 1".
- `invalidateIndex()` מנוקה אחרי POST/PATCH/DELETE, כך שרשומה חדשה נפתרת לשם מיד.
- `createRecord`/`updateRecord` מחזירים כעת גם `id` (קודם הוחזרו `fields` בלבד).

**מדידה אחרי (סריקת כל 25 הטבלאות):**
- `עבודות עובדים`: 10.1s → **0.28s**. `מבנים`: 4.1s → 0.54s. `חשבוניות`: 4.2s → 0.26s.
- כל 25 הטבלאות מתחת לשנייה; העשרת הקישורים עולה אפס (raw ו-enriched זהים).
- **אפס** Record IDs גולמיים דולפים ללקוח — נבדק על כל הטבלאות.

**בנוסף:** `start.bat` היה קובץ ריק (0 בייט) — אין דרך הרצה. נכתב מחדש (בדיקת .env,
התקנת חבילות חסרות, הרצת שרת+לקוח, פתיחת דפדפן). `SCHEMA.md` רוענן — 25 טבלאות בפועל,
לא 26 (`רשומות הוצאה` אינה קיימת יותר ב-Base). נוקו קבצי אבחון זמניים.

## ✅ תוכנית שתילה — נכתב מחדש לפי סעיף 44 (2026-08-26)

האפיון המלא נמצא בשורות 2290–3617 ב-`סיכום איפיון-סגור.txt`. המסך הקודם היה שלד
עם קריסות ודאיות. מה שהיה שבור:

1. **`load()` לא הוגדר מעולם** — נקרא אחרי "הזז תוכנית" ואחרי "צור תוכנית"
   (שורות 338, 404 בגרסה הישנה). כל פעולת כתיבה מוצלחת קרסה מיד לאחריה ב-ReferenceError.
2. **`מספר ימי הזזה` נכתב לטבלת `תקופות תוכנית`** — השדה כלל לא קיים שם, הוא שייך
   ל-`תוכניות שתילה`. ההזזה נכשלה תמיד.
3. **שם שדה שגוי `קג צפוי`** — השדה האמיתי הוא `ק"ג צפוי` (עם גרשיים). העמודה
   הציגה "טרם" תמיד. כנ"ל `סוג חג` במקום `סוג החג`.
4. **`buildMonthGrid` פתח תמיד בינואר** ולא בחודש הנוכחי; כפתור "היום" החזיר לינואר.
5. **בחירת "כל השנים"** נתנה `Number('הכל')` = NaN → תאריך לא חוקי → קריסת רינדור.

מה יש עכשיו (לפי האפיון):
- **פילטרים משותפים** (סעיף 4): שנה / מבנה / גידול / רבעון — נשמרים במעבר בין הטאבים,
  עם "נקה פילטר". רשימות המבנים והגידולים נגזרות מתוכניות השנה בפועל.
- **בניית תוכנית** (9–13): לוח חודש/שבוע עם קודם/היום/הבא וקפיצה מהירה Q1–Q4, כותרת
  רבעון בצבע האפיון. כל אירוע מציג אייקון גידול + מבנה + גידול + "שתילה"/"קטיף"
  (סעיף 12 — לא הסתמכות על צבע בלבד). עבר בשקיפות, היום במסגרת, ימי אי עבודה מסומנים.
- **כרטיס תוכנית** (14–15): "תכנון מקורי" מול "תוכנית מעודכנת" זה לצד זה, Badge
  "עודכן"/"ללא שינוי" והדגשת התאריך המעודכן בכחול — המקורי לעולם אינו נדרס.
  טבלת "תחזית וביצוע" ממוינת לפי תחילת שבוע, וטבלת תקופות עם Badge "ידני".
- **פעולות** (16–20): תוכנית חדשה / עריכה / שכפול / הזזה / שינוי תקופה. כולן כותבות
  ל-Airtable בלבד ומפעילות את ה-checkbox הקיים (`חשב תוכנית`, `הזז תוכנית`) —
  Zite אינו מחשב תאריכים. אומת שכל שדות האוטומציה הם checkbox (ניתנים לכתיבה)
  ושהאפשרות `שינוי ידני` קיימת ב-singleSelect של `מקור`.
- **תכנון מול ביצוע** (22–27): שבועות מ-Airtable (`תחילת שבוע`/`סוף שבוע` — לא מחושב),
  Badge רבעון, צפוי כחול מול בפועל ירוק כהה זה לצד זה, ו-Drawer "פרטי שבוע"
  עם 3 סיכומים ופירוט לפי מבנים.
- **דשבורד** (28–40): 5 KPI + 7 שורות גרפים בסדר שנקבע באפיון, עם Drill-down
  (לחיצה על שבוע/מבנה/גידול/רבעון מסננת או פותחת פרטים).
- **"טרם התקבל ביצוע"** (סעיף 24): 0 אינו מוצג כביצוע אמיתי בשבוע שטרם הסתיים.
- **חצית שנה** (סעיף 43): הלוח נפתח אוטומטית בחודש שבו הפעילות באמת מתחילה,
  ומוצג חיווי כשהתאריכים חורגים מהשנה הנבחרת.

**אומת מול Airtable החי:** 2 אירועי לוח, 3 שבועות תחזית, סה"כ 1,007 ק"ג צפוי
ו-₪5,034 הכנסה צפויה לשנת 2026 — כל שדות התחזית מאוכלסים. הלקוח נבנה נקי.

⚠️ **לתשומת לבך:** ב-Airtable קיימים שאריות נתוני בדיקה — גידול בשם
`__PLANT_TEST_1786951792666__` ותוכנית לשנת 2099 (מבנה 6). לא נגעתי בהם;
כדאי למחוק אותם ידנית מ-Airtable.

## ✅ סבב סקירה ויצוב (2026-08-27)

המשך סקירת כל המסכים מול האפיון והדבקת פערים:

- **שרת (`server.js`)** — תוקן באג סדר נתיבים: `GET /api/select-options/:table/:field` ובעיקר
  `POST /api/upload-document` נקבעו **לפני** הנתיב הגנרי `/:table`, כך שהעלאת מסמך אינה נקלטת
  יותר כניסיון ליצור רשומה בטבלה "upload-document". נוסף מטמון קריאה קצר (30 ש"נ) עם
  `invalidateReads` (מנקה גם את אינדקס השמות) — כל 25 הטבלאות נטענות בקפיצה.
- **`/api/select-options`** — נבדק חי: `ימי אי עבודה/סוג החג` → `["תילאנדי","יהודי"]`.
- **פרוב אוטומטי של מטמון+כתיבה חי**: קריאה ראשונה MISS, שניה HIT, יצירת רשומה 201 + MISS,
  ומחיקת רשומת הבדיקה. (כישלון מוקדם של הפרוב נבע מקידוד UTF-8 שגוי ב-PowerShell, לא מהשרת.)
- **NonWorkDaysPage** — "סוג החג" נטען כעת מ-API (ולא מקודד) וטופס ההוספה מוגבל לאפשרויות
  האמיתיות של ה-singleSelect, כך שכתיבה ל-Airtable לא נדחית בשגיאת הרשאות.
- **FinancePage**:
  - *צ'קים (סעיף 26)*: נוספו ה-KPI "לפירעון השבוע" ו"לפירעון החודש" (לצד "סכום לפירעון"
    ו"מבוטלים") — ברמת האפיון. הכפתור "הצג מבוטלים" נשמר. ללא מחיקת רשומות.
  - *משווקים (סעיף 27)*: פדיון בתקופה מחושב אמיתי (סיכום `סכום נטו` של החשבוניות המקושרות
    לפי id), הצגת מספר חשבוניות, וגרף "פדיון לאורך זמן" שנפתח בלחיצה על כרטיס.
- **UploadDocumentPage** — תוקן תו קידוד מקולקל (`₩` → `📤`).
- **סקירת מסכים (Spraying/SprayReports/Treatments/WeeklySummary/Login/Upload)** — אומתו מול
  סעיפים 19–22, 21, 30–34: טופס ריסוס מלא, פירוק JSON של דוחות ריסוסים, תמיכה בטווחי
  תאריכים + הדגשת היום בלוח הטיפולים, פרסור נקי של 5 ה-JSON בסיכום השבועי.
- הלקוח נבנה נקי (vite build) לאחר כל השינויים.

## ✅ סבב סקירת שוק (2026-08-27) — חשבוניות, תעודות, דשבורד, מסכים

- **InvoicesPage** (סעיף 28): Drawer החשבונית הועשר ל-7 KPI (ברוטו/נטו/משקל/קרטונים/משטחים/
  ניכוי/הובלה) ו-4 גרפים — "פדיון לפי יום", "משקל לפי יום", "קרטונים לפי יום" (Bar) ו"פדיון לפי זן" (Pie) — כולם על שמות שדות חיים.
- **DeliveryNotesPage** (סעיף 29): Drawer התעודה הועשר — "משקל לפי יום" + "קרטונים לפי יום"
  (Bar) ועוגות "משקל/קרטונים/משטחים לפי זן" (Pie), פירוק נקי של "סיכום יומי" (days/products).
- **FinancePage/UploadDocumentPage** — חידוד נוסף (פירוט בסבב הקודם).
- **סקירת מסכים שנותרו מול האפיון** — כולם תקינים, בעלי KPI + גרפים עם עקומת מדרגה ותוויות
  במקרא (מונע חפיפה), לפי סעיפים:
  - **AlertsPage** (35): KPI + עוגת "חריגות לפי סוג" + עמודות "לפי שבוע" + טבלה מפורטת + מלאי נמוך + Drawer שבוע.
  - **CropsPage** (37–40): 4 טאבים (גידולים/מחירי גידול/תפוקה רבעונית/תחזית שתילה) עם גרפים.
  - **StructuresPage** (11): כרטיסים + Drawer עם 8 טאבים, בהם תפוקה (עוגת סוג קטיף + עמודות ק"ג) וכספים.
  - **TeamCrewPage** (13): פילטרים + KPI + כרטיסי עובדים + 5 גרפים (כולל Horizontal).
  - **WorkersPage** (12): כרטיסים + Drawer עובד עם 6 KPI + 6 גרפים.
  - **DashboardPage**: KPI (ברוטו/נטו/הוצאות/רווח/ק"ג) + Area הכנסות לאורך זמן + עוגת הוצאות לפי קטגוריה + מבנים פעילים.
- כל הגרפים משתמשים במודול המשותף `chart.js` — כיתובים מיובלים במדרגה בעת הצורך, ושמות פלחים
  במקרא בלבד (לא תוויות על פלחי העוגה) — הכיתובים אינם עומדים זה על זה או על קווי הרשת.
- הלקוח נבנה נקי (vite build, 628 מודולים).

## 🚧 נותר (לבחינה/המשך)
2. **התחברות עובד — קוד אימות אמיתי** — כרגע נעשה שימוש בקוד מקומי זמני (1234 / 4 ספרות מספר עובד). דורש שרת אימייל לשליחת קוד.
3. **העשרת Global.css** — יש לוודא שכל מחלקות ה-CSS החדשות (tabs, drawer-body יישור, worker-header) מעוצבות במלואן.
4. **רשומות הוצאה** (סעיף 43) — ניתן להוסיף כטאב בכרטיס הוצאה.

---

## ⏱️ הערכת עבודה כוללת
החלקים העיקריים (P1–P6 + גידולים + ימי אי עבודה) בוצעו בסבב זה. נותרו בעיקר עיבודים עמוקים יותר (PlaningPlan, spraying form, calendar, finance tabs, suppliers) — כ־8–14 שעות עבודה נוספות לפי הצורך.

---

## 🔗 המשך משיחה חדשה
1. פתח שיחת Claude Code בתיקיית `project-dvir`.
2. אמור: "קרא PROGRESS.md והמשך מהנקודה האחרונה".
3. המשך מהסעיף הלא־מושלם (PROGRESS.md + הקבצים בפועל על הדיסק).

---

## ✅ סבב "רשימת-משימות-NEXT" (2026-08-31)

**שלב 1 — שרת:** התהליך הישן הוחלף בשרת נקי; כל הטבלאות הקיימות מחזירות 200 עם נתונים
אמיתיים. נוסף middleware שמחזיר **404 עם הודעה בעברית** לטבלה שאינה קיימת ב-Base
(במקום 403 העמום של Airtable), עם מטמון שמות-טבלאות ל-5 דקות.

**שלב 8 — תכנון טיפולים (נכתב מחדש):** שורש הבעיה — התאריך מ-Airtable מגיע בפורמט ISO
("2026-08-04T...") והקוד פירק אותו לפי `-` כאילו הוא טווח, ולכן שום טיפול לא הוצג.
עכשיו: פרסור נכון (ISO / טווח "DD/MM/YYYY-DD/MM/YYYY"), KPI לפי סעיף 25, פילטרים
(שנה/חודש/שבוע/מבנה/גידול/זן/סוג), לוח חודש/שבוע/רשימה, צבע לפי סוג טיפול,
הדגשת "היום/מתחיל היום/יום אחרון", Chips למבנים מרובים, ויצירה/עריכה/מחיקה/סימון-בוצע
מלאים מול Airtable (טופס עם select-options חיים, טווח תאריכים, ותוכנית שתילה לגידול/זן).

**שלב 9 — בקשות עובדים (מודול חדש):**
- `client/src/utils/requests.jsx` — קבועים משותפים + מסך הסבר כשהטבלה חסרה.
- `client/src/pages/WorkerRequestsPage.jsx` — מסך מנהל: KPI ממתינות/אושרו/לא אושרו,
  טאבים, פילטרים (עובד/סוג/תאריכים/חיפוש חופשי), כרטיסים, Drawer אישור/דחייה עם הערה,
  checkbox "מאפשר הזנת עבודה לתאריך", ורענון מחזורי כל דקה. נתיב `/requests` (גם למנהל עבודה).
- `client/src/worker/WorkerRequests.jsx` — "הבקשות שלי" לעובד: יצירה (חופש/מחלה,
  חלק מהיום עם משעה/עד-שעה/עד-סוף-היום), סטטוס בצבע+טקסט, תשובת מנהל,
  "הסר מהתצוגה שלי", וכפתור "הזנת עבודה" לבקשה מאושרת — עברית/תאילנדית מלא.
- `WorkerReport` — תאריך העבודה נעול להיום (הכלל הסופי באיפיון); נפתח לעריכה רק
  דרך בקשה מאושרת (`approvedDate`).
- ⚠️ **הטבלה "בקשות עובדים" עדיין לא קיימת ב-Airtable** — ל-PAT אין הרשאת
  `schema.bases:write` (הניסיון החזיר 403). הסקריפט מוכן: `server/src/_create-requests-table.mjs`
  (רץ אחרי הוספת ההרשאה ל-PAT, או ליצור ידנית לפי הרשימה שמוצגת במסך).

**שלב 5 — לוח שנה, שבתות וחגים:**
- `client/src/utils/holidays.js` — חישוב חגי ישראל מהלוח העברי (Intl, בלי ספרייה),
  חגי תאילנד קבועים, שבתות, שמות he/th, וצבע לכל סוג (יהודי כחול / תאילנדי כתום / שבת סגול).
- לוח תוכנית השתילה: תא שלם צבוע לפי הסוג + שם החג בתוך התא + מקרא.
- `NonWorkDaysPage` נכתב מחדש: רשימה + לוח שנתי (לחיצה על יום = הוספה/עריכה),
  עריכה/מחיקה עם אישור, ייבוא חגי ישראל / חגי תאילנד / שבתות לשנה (מדלג על קיימים,
  מדווח כמה נוצרו/נכשלו), KPI לפי סוג.

**שלב 10 — העלאת מסמך (נכתב מחדש לפי "גרסה סופית"):** 4 שלבים עם אישור שבוע חובה
לחשבונית הכנסה/תעודת משלוח — "✓ השבוע נכון" / "שנה שבוע" (שבוע קודם/הבא, שבת→חמישי,
קוד YYYYMMDD-YYYYMMDD שלא ניתן להקליד ידנית), Drag&Drop + צילום מהטלפון (capture),
בדיקת סוג וגודל קובץ, Preview לתמונות, כרטיס סיכום לפני שליחה, והיסטוריה עם קוד שבוע
וקישור לקובץ.

**שלב 7 — CRUD למנהל ראשי:** רכיב גנרי חדש `client/src/components/RecordForm.jsx`
(אפשרויות select מהמטא, לא שולח שדות ריקים, שגיאות בעברית) + `removeRecord` עם אישור.
חובר ל: **מבנים** (הוספה/עריכה/מחיקה + כפתורי 👁/✎/🗑 בכרטיס), **עובדים**, **גידולים**.
מוצג רק ל-owner; מנהל עבודה רואה תפעול בלבד (`/requests` נוסף ל-OPERATIONS).

**שלב 6 — תאילנדית:** `data-lang` על שורש המסמך + בלוק CSS ייעודי — גופן גדול יותר,
גובה שורה 1.8, ריווח תאים/ניווט/כפתורים נדיב יותר בתאילנדית. כל מודול הבקשות תורגם
(עשרות מפתחות i18n חדשים he/th).

**שלב 4 — תוכנית שתילה:** רענון אחרי פעולה הפך "שקט" — לא מציג שלד ולא מאפס
טאב/מיקום (נשארים באותו עמוד); היצירה נכתבת ל-Airtable ומופיעה מיד לאחר `load`.

**עוד:** טולטיפ הדשבורד כבר בעברית ("פדיון נטו"); build נקי — 634 מודולים.

**נותר פתוח:**
1. יצירת טבלת "בקשות עובדים" ב-Airtable (דורש הרשאת סכמה ל-PAT או יצירה ידנית).
2. תרגום תאילנדי מלא של גוף מסכי המנהל הוותיקים (הניווט, אפליקציית העובד והבקשות מתורגמים).
3. "סיכום שבועי" ב-Airtable מכיל רשומות עם `#ERROR!` בתאריכים — כדאי לתקן ב-Base.

---

## 2026-08-31 — צ'קים (סעיף 26 באיפיון) — השלמה מלאה

**באג שתוקן:** שמות השדות בקוד היו עם גרש לטיני (`סכום צ'ק`, `שם בעל הצ'ק`, `ספק`)
בעוד שב-Airtable הם `סכום צ׳ק` (גרש עברי), `שם בעל הצק`, `ספקים` — לכן הסכומים והספקים
הוצגו תמיד "לא זמין". כל הגישה לשדות רוכזה ב-`client/src/utils/checks.js` (עם fallback לשני הכתיבים).

**חדש — `client/src/components/ChecksTab.jsx`** (מחליף את הטאב הישן ב-FinancePage):
- KPI: לפירעון השבוע / לפירעון החודש / סכום לפירעון (+מספר ממתינים) / מבוטלים.
- פילטרים לפי האיפיון: שנה, חודש, טווח תאריך פירעון, ספק, סטטוס — עובדים **יחד** עם
  חיפוש חופשי (מוטב / ספק / סטטוס / בעל הצ'ק / מספר). "נקה פילטרים".
- מתג "הצג מבוטלים" (מבוטלים מוסתרים כברירת מחדל; מונה כמה מוסתרים).
- טבלה עם כל העמודות: צילום (ממוזער, לחיצה = הגדלה), מס׳, מוטב, שם בעל הצ׳ק, סכום,
  תאריך פירעון (+תג "באיחור"), ספק, חשבוניות, סטטוס, הערות. מיון לפי פירעון, "הצג עוד" (50).
- מגירת צ'ק: צילום גדול, פרטים, ימים לפירעון, קישורים "פתח ספק ←" (`/suppliers?supplier=<id>`)
  ו"פתח חשבונית ←" (`/invoices?invoice=<id>`) — כפתור החזרה מחזיר למסך הכספים בטאב הצ'קים.
- סימון צ'ק: "סמן כנפרע" / "סמן כלא נפרע" / "בטל צ׳ק" (עם אישור) → PATCH + קריאה מחדש
  של הצ'קים. **אין מחיקה** — לפי האיפיון.
- `/finance?tab=checks` פותח ישירות את הטאב; `&supplier=<שם>` מסנן לפי ספק.

**SuppliersPage:** טאב צ'קים בכרטיס ספק תוקן (שדה `ספקים`), מציג מס׳/מוטב/סכום/פירעון/סטטוס
+ סה"כ, וכפתור לכל צ'קי הספק במסך כספים. קישור עמוק `?supplier=<id>`.
**InvoicesPage:** קישור עמוק `?invoice=<id>` פותח את מגירת החשבונית.

**⚠️ נותר צעד ידני ב-Airtable:** לשדה "סטטוס" בטבלת צ׳קים יש רק "נפרע" / "לא נפרע".
האיפיון דורש גם "מבוטל". ל-PAT אין הרשאת סכמה, לכן יש להוסיף פעם אחת את האפשרות
**מבוטל** לשדה ידנית. עד אז כפתור "בטל צ׳ק" מושבת עם הסבר, וכל השאר עובד.

## ✅ סבב — סיכום שבועי (סעיפים 30–35) והעלאת מסמכים (36 + "גרסה סופית") — 2026-08-31

**שרת:** `GET /api/:table` תומך כעת ב-`?fields=a,b,c` (נכלל במפתח המטמון) — רשימות יכולות לוותר על שדות JSON כבדים. *דורש הפעלה מחדש של השרת (start.bat).*

**WeeklySummaryPage — נכתב מחדש:**
- הרשימה טוענת רק 11 שדות קלים (בלי JSON); כרטיס השבוע נטען טרי מ-Airtable (`GET /api/סיכום שבועי/:id`) בכל פתיחה, מתרענן כל 30 ש' בזמן שהוא פתוח + כפתור 🔄 "עודכן HH:MM" — לפי "JSON וסיכום שבועי" באיפיון.
- 7 טאבים (נוסף **מסמכים**): סקירה (7 KPI כולל מספר חשבוניות/תעודות + סיכום בדיקה יומית), לפי ימים (טבלה + 4 גרפים: פדיון/קרטונים/משקל/משטחים), זנים (מ-`days[].products`: טבלה, הכנסה/משקל לפי זן, קרטונים לפי זן לאורך זמן), מבנים (כרטיסים עם הכנסה נטו/חלק יחסי/זנים, הכנסה וקרטונים לפי מבנה, קרטונים לפי מבנה לאורך זמן, מטריצת מבנה×זן), ק"ג בפועל (5 KPI + 7 גרפים + טבלה עם rowSpan לפי סעיף 34), התאמות (פירוט יומי מ-`JSON בדיקת התאמה יומית` ו-`JSON התאמת קטיף` עם תוויות בעברית לסוגי החריגה), מסמכים (חשבוניות מה-`JSON חשבוניות מאוחד` + רשומות עם קישור לקובץ, תעודות משלוח של השבוע, מאוחד לפי יום).
- **תוקנו פרסרים לפי המבנה האמיתי ב-Airtable:** פדיון יומי הוא `gross_sales_amount` (הוצג 0), הכנסה לפי מבנה היא `structures[].net_income` (הוצג ריק), סיכום קטיף לפי מבנים הוא `{structures:[…]}` ולא מערך, זנים נלקחים מ-`days[].products` (השדה `varieties` העליון לא קיים). `parseJSON` סובלני ל-`\_` מוברח שמופיע בנתונים.
- ערכי `{error:'#ERROR!'}` מ-Airtable מנוקים (`cleanRecord`), ו-`formatDate` מחזיר "לא זמין" לאובייקט.

**UploadDocumentPage:**
- הצלחה לפי האיפיון: "✓ המסמך הועלה בהצלחה" + "נשלח לעיבוד", Spinner בכפתור (`.spinner` ב-global.css), הצלחה רק אחרי שהתקבל id של הרשומה.
- **מעקב עיבוד:** אחרי ההעלאה הרשומה שנוצרה נקראת מחדש כל 6 ש' (עד 4 דק') ושדות ה-Automation (תאריך/משווק/סכומים/משקל/קרטונים · ספק/קטגוריה · Attachment Summary) מוצגים ברגע שמולאו — בלי רענון ידני, בלי Optimistic UI.
- היסטוריה "מסמכים שהועלו לאחרונה": נושא, קובץ, תאריך העלאה, תאריך מסמך, שבוע, **סטטוס** ("נותח" אם שדה ניתוח מולא, אחרת "ממתין לעיבוד" — נגזר משדות אמיתיים בלבד) + כפתור רענון.

**DashboardPage:** כפתור פעולה מהירה "⬆️ העלאת מסמך" ב-topbar, ושורת "בקרת מסמכים" (התראות פעילות / חסרים מסמכים / אי התאמות / שגיאות חישוב) מתוך שדות הסטטוס של "סיכום שבועי", לחיצה → חריגות/סיכום שבועי.

**נשאר לשיקול:** נושא "צ'ק" במסך ההעלאה נוסף בסבב קודם לפי רשימת המשימות של המשתמש; האיפיון מונה 4 נושאים בלבד — הושאר.

## ✅ סבב תעודות משלוח (2026-08-31) — סעיף 29 מלא + כללי אובייקטים/טבלאות

**קבצים חדשים:**
- `client/src/utils/deliveryNotes.js` — מקור אמת יחיד לשדות התעודה (`noteDate/noteCartons/noteWeight/noteAvg/noteDeviation/noteCheck/noteWeekCode/noteMarketer/noteStructure/noteDocument`), `parseDailySummary` לפי המבנה האמיתי (`days[] → products[] / shipments[]`, כולל גרסה ישנה עם products בשורש), וסינון `notesOfMarketer/notesOfStructure/notesOfWeek`. null נשאר null ("לא זמין").
- `client/src/utils/table.js` — מיון יציב (ריקים בסוף), טווח תאריכים, עימוד, ייצוא CSV (BOM לאקסל).
- `client/src/components/DeliveryNoteDrawer.jsx` — כרטיס תעודה מלא עם **מחסנית אובייקטים + Breadcrumb + "חזרה"**: תעודה → משווק / מבנה / שבוע (כל אחד נטען מ-Airtable ומציג את התעודות שלו, וממנו אפשר לפתוח תעודה נוספת). בכרטיס: 4 KPI, פרטים (רק שדות מלאים), המסמך המצורף (תצוגה מקדימה + "פתח מסמך"), סיכום יומי כ-**יום → זנים → משלוחים** (שורות נפתחות), ו-5 הגרפים שבאיפיון (משקל/קרטונים לפי יום, משקל/קרטונים/משטחים לפי זן). "פתח עמוד מלא" מנווט החוצה רק בבחירה מפורשת. מייצא גם `ObjChip` ו-`CheckBadge` לשימוש חוזר.

**DeliveryNotesPage (נכתב מחדש):** עמודות לפי האיפיון (תאריך, משווק, מבנה, שבוע, קרטונים, משקל, ק"ג לקרטון, סטיית משקל, בדיקת משקל, מסמך); חיפוש (משווק/קוד שבוע/מס' תעודה/שם קובץ) שעובד יחד עם הפילטרים (משווק, מבנה, שבוע, בדיקת משקל/חריגות בלבד, מתאריך/עד); מיון בלחיצה על כותרת; עימוד (25/50/100); הדפסה (CSS ייעודי — מדפיס את כל השורות המסוננות) וייצוא CSV של המוצג; 5 KPI על הסינון (KPI "חריגות משקל" לחיץ ומסנן); Chips לחיצים למשווק/מבנה/שבוע; עריכה ל-owner דרך `RecordForm`; פרמטרי URL `?open= ?marketer= ?structure= ?week=`.

**חיבורים במסכים אחרים:**
- **StructuresPage** — טאב "מסמכים" בכרטיס מבנה מציג את תעודות המשלוח של המבנה (KPI + טבלה + קובץ) וקבצי המבנה (סקיצה/מצגות); לחיצה פותחת את כרטיס התעודה.
- **FinancePage › משווקים** — כרטיס משווק מציג גם "תעודות משלוח" (ספירה) וכפתורים לתעודות/חשבוניות של המשווק.
- **WeeklySummaryPage › מסמכים** — שורת תעודה לחיצה ופותחת את כרטיס התעודה (התעודות נטענות עם שמות מקושרים, לא raw); `?week=` פותח את כרטיס השבוע.
- **InvoicesPage** — בכרטיס חשבונית: "תעודות משלוח" מקושרות כ-Chips (→ `/delivery-notes?open=`), שבוע לחיץ (→ `/weekly?week=`), משווק מוצג דרך `displayName` (לא אובייקט גולמי).
- **AlertsPage** — בכרטיס שבוע: "תעודות משלוח של השבוע" ו"פתח סיכום שבועי" (קישור מהתראה לאובייקט).
- **UploadDocumentPage** — מקבל `state.docType` ומסמן מראש "תעודת משלוח" כשמגיעים מכפתור "העלאת תעודה".
- `global.css` — `.obj-chip`, `.crumbs`, טבלאות מקוננות, מיון בכותרת, `.pager`, ו-`@media print` כללי (מסתיר סרגל צד/פילטרים/מגירות).

**נשאר לשיקול:** Feed "פעילות אחרונה" בדשבורד (כולל תעודות משלוח) — לא בוצע בסבב זה.

## ✅ תוכנית שתילה — הגיליון השנתי (סרגלים אנכי ורוחבי) (2026-08-31)

**הבקשה:** הלקוח רגיל לקובץ האקסל שלו ("To be or not to be") — סרגל רוחבי של שנה → חודשים →
שבועות (52 + המשך לשנה הבאה), שורות זנים / חגים / הערות, סרגל אנכי של המבנים עם הדונם,
תאים צהובים לשתילה וירוקים לקטיף עם הק"ג, ומתחת למבנים שורות הנתונים (ק"ג שבועי, משטח שבועי,
סה"כ לחודש, סה"כ שנתי, הכנסות בפועל). הוא לא ראה את "תכנון מול ביצוע" ואת טבלת הנתונים —
בפועל השרת (4000) לא רץ אצלו, וגם הטבלה הוצגה רק בטאב "דשבורד".

**מה נעשה** (`client/src/pages/PlantingPlanPage.jsx` — רכיב `PlanSheet`, CSS `.plan-sheet` ב-`global.css`):
- טאב "בניית תוכנית" נפתח כברירת מחדל ב**גיליון שנתי**; מתג תצוגה "גיליון שנתי / חודש / שבוע"
  משאיר את לוח השנה הקודם כאופציה. שאר הטאבים (תכנון מול ביצוע, דשבורד + טבלת אקסל) נשארו.
- סרגל רוחבי דביק (sticky): שנה+חודשים, שבועות (מספר + תאריך יום ראשון), זנים (הגידולים
  בקטיף באותם שבועות, ממוזגים), חגים ומועדים (מ"ימי אי עבודה" + הלוח העברי, בלי שבתות), הערות
  (תאריכי החגים). השבוע הנוכחי מודגש. השבועות נמשכים לשנה הבאה עד סוף התוכנית האחרונה.
- סרגל אנכי דביק: כל המבנים ממוינים לפי מספר, עם שטח בדונם.
- תאים: צהוב = משתילה עד תחילת הקטיף (בתא הראשון "תאריך + גידול", כמו "5.2 מלפפון"), ירוק =
  שבועות קטיף עם ק"ג צפוי מ"תחזית שתילה שבועית" ומתחתיו **ק"ג בפועל** בירוק כהה כשהתקבל.
  התקופות מגיעות מ"תקופות תוכנית" (כולל שינוי ידני), עם נסיגה לתאריכי התוכנית. לחיצה על תא
  פותחת את כרטיס התוכנית; לחיצה על שבוע/ק"ג שבועי פותחת את "פרטי שבוע".
- שורות סיכום מתחת למבנים: ק"ג שבועי, ק"ג בפועל, משטח שבועי (690 ק"ג למשטח — לפי הגיליון),
  הכנסה צפויה ₪, סה"כ לחודש ₪ (ממוזג לפי חודש), סה"כ שנתי ₪ (לשנה ולהמשך), הכנסות בפועל ₪
  (ק"ג בפועל × מחיר לק"ג). שורת "כפול 3 נטו" לא הועתקה — במקומה ההכנסה הצפויה מ-Airtable.
- הפילטרים (מבנה/גידול/רבעון) חלים על הגיליון: רבעון מצמצם את העמודות לרבעון.

**אומת:** `npx vite build` נקי; צילומי מסך headless (Edge/CDP) מול Airtable החי — 12 מבנים,
5 תוכניות עם תאריכים ל-2026, 62 שבועות (כולל המשך ל-2027 בגלל תוכנית 3).
